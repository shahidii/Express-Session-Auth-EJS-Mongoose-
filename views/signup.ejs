<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Signup</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous"/>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"/>
    <link rel="stylesheet" href="/Signup/styles.css">
</head>
<body>
    <div class="signup-container">
        <div class="signup-form">
            <h2>User Signup</h2>

            <div class="validation-status mt-3">
                <div class="validation-item" id="nameValidation">
                    <span class="circle invalid"></span>
                    Name must be at least 3 characters
                </div>
                <div class="validation-item" id="emailValidation">
                    <span class="circle invalid"></span>
                    Valid email required
                </div>
                <div class="validation-item" id="passwordLengthValidation">
                    <span class="circle invalid"></span>
                    Password must be at least 8 characters
                </div>
                <div class="validation-item" id="passwordNumberValidation">
                    <span class="circle invalid"></span>
                    Password must include at least 1 number
                </div>
                <div class="validation-item" id="passwordSpecialValidation">
                    <span class="circle invalid"></span>
                    Password must have at least 1 special character
                </div>
            </div>
            <div class="alert alert-danger mt-2" id="hoverAlert" style="display:none;"></div>
            <% if(typeof error !== 'undefined') { %>
                <div class="alert alert-danger mt-2" id="hoverAlert">
                    <%= error %>
                </div>
            <% } %>

            <form id="signupForm" action="/signup" method="POST">
                <div class="form-group mb-3">
                    <label for="name">Name</label>
                    <input type="text" class="form-control" id="name" name="name" required />
                </div>
                <div class="form-group mb-3">
                    <label for="email">Email</label>
                    <input type="email" class="form-control" id="email" name="email" required />
                </div>
                <div class="form-group mb-4">
                    <label for="password">Password</label>
                    <input type="password" class="form-control" id="password" name="password" required />
                </div>
                <button type="submit" class="btn btn-primary w-100" id="signupButton" disabled>Sign Up</button>
                <div class="extra-links mt-3">
                    <!-- <p>Are you an employee? <a href="/signup">Sign up as employee</a></p> -->
                    <p>Already have an account? <a href="/login">Login here</a></p>
                </div>
            </form>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
    <script src="/Signup/script.js"></script>
</body>
</html>
